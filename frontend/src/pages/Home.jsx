import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Typography variant="h4" mt={5}>Insurance DApp</Typography>
      <Typography>Decentralized Insurance Management System</Typography>
    </Container>
  );
};

export default Home;
